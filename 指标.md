# FedScale

## Research

相关论文参见[papers](./docs/papers)

[Fedml](./docs/FedML_report_cn.md)问答解析。

[DiLoCo](./docs/DiLoCo_report_cn.md)问答解析。

[OpenDiLoCol](./docs/OpenDiLoCo_report_cn.md)问答解析。



大模型微调[测试](./docs/AlpacaGPT4_SFT_Evaluation_Principles_ROUGE_BERTScore_Loss_PPL.md)解答。


## Design

[FedScale](./designing/FedScale_v1_detailed_design.md)当前设计。

当前设计中一些困惑的[讨论](./designing/FedScale_DDP_FSDP_CanonicalBlock_Concise.md)。



## Implementation
### Flowers

[Flowers](./implementing/Flowers_DESIGN.md)当前实现架构。


### 任务分工

| 阶段         | 时间             | 目标                           | 备注     |
| :---------- | :-------------- | :----------------------------- | :--------------------- | 
| **第一阶段** | 7.1 – 7.25  | **FSDP基线**<br>把DDP换成FSDP，跑通         | ① FSDP替换DDP ② 1B/3B/7B跑通 ③ baseline评估    |
| **第二阶段** | 7.21 – 8.25 | **实现设计**<br>核心：验证设计是否可行 | ① Bridge设计；② FSDP侧：shard→block映射 ③ 明文闭环，准确性对比 |
| **第三阶段** | 7.11 – 8.30 | **最小版本实现**<br>实现评估           | ① Public Mask、INT8量化 聚合正确性验证 ② **相关工作复现和实验对比** <br>③ 聚合正确性验证 |
| **论文月**  | 8.15 – 9.30 | **写论文+补实验**         | ① 补全评估和实验对比；②  画图；③ 写实验素材      |



#### 第一阶段 TODO List


* [x] 实现单智算中心（ICC）内部的 DDP 分布式训练。
* [ ] 实现单智算中心内部的 FSDP 分布式训练。
- [ ] 评估两种模式下的集群内训练性能：
  - 前向传播时间，反向传播时间；
  - 集群内通信时间；
      - DDP: All-Reduce；
      - FSDP: All-Gather + Reduce-Scatter + 可选reshard / prefetch 时间；
  - 优化器更新时间；
  - 单 step 时间；
  - 总训练时间，吞吐量（tokens/s）。
- [ ] 评估两种模式下的集群内资源使用：
  - GPU 显存峰值与 GPU 利用率；
  - CPU 利用率与 CPU 内存峰值；
  - 网络通信流量与 NCCL 开销；
  - FSDP 状态导出开销，例如 full-state export 或 sharded-state export；
  - checkpoint 保存、恢复和状态导出开销(如有)。 
- [ ] 两种模式下的跨智算中心联邦更新
  - 本地模型更新（model delta）导出时间：ICC 内部把训练状态转换为可上传的 model delta 的时间；
  - 集群间模型更新传输时间：model delta 通过跨中心网络发送到聚合端的时间；
  - 明文 FedAvg 聚合时间：服务端接收多个 ICC 的更新后计算平均更新的时间；
  - 单次联邦轮总时间。
- [ ] 两种模式模型微调的准确度，给出评价指标和方法
  - validation loss，perplexity；
  - 根据具体任务补充下游指标，例如 Accuracy、Macro-F1、Exact Match、ROUGE-L 或 BERTScore；
  - 比较 DDP 与 FSDP 在相同 global batch size、训练步数和随机种子下的最终模型质量。

#### 第一阶段指标采集修复说明（2026-08）

训练镜像后续版本将：

- 直接围绕 `Trainer.compute_loss` 记录前向/损失计算时间，避免当前 TRL 回调路径未触发时显示为 0；
- 通过 NVML 或 `nvidia-smi` 采集 GPU 利用率，采集不可用时记录为空，不再用 0 伪装；
- 按 `all_reduce`、`all_gather`、`reduce_scatter`、`all_to_all_single` 分项记录通信时间、流量和调用次数；
- 记录服务端纯 FedAvg 聚合时间、checkpoint 保存时间/大小，以及初始状态或恢复加载时间；
- 保留 model-delta 导出时间和大小。

跨智算中心网络传输的纯传输耗时仍未由 Flower 应用层暴露，不能用客户端整轮剩余时间代替；模型质量指标需要提交实验时开启 `train.evaluate-after-fit=true`，或对最终 checkpoint 执行独立评估任务。

